[main]
# This deployment currently has no usable IPv6 egress path, so prefer IPv4 MX
# targets first and only fall back to IPv6 when needed.
inet_prefer = v4

# Keep loop protection explicit for clarity.
local_mx_ok = false
